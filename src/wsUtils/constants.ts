const SOCKET_URL = window.location.protocol + '//' + window.location.hostname + ':9005/ws'

const SHIFT_EVENTS_TOPIC = '/topic/shift.events'

const TASK_EVENTS_TOPIC = '/topic/task.events'

export {
  SOCKET_URL,
  SHIFT_EVENTS_TOPIC,
  TASK_EVENTS_TOPIC
};

